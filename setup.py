#!/usr/bin/env python
from distutils.core import setup

setup(
        name = 's7n-pages',
        version = "1a1",
        packages = ['s7n', 's7n.pages'],
        include_package_data = True,
        )
